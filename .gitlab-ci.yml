stages:
  - test_code
  - build
  - test_package
  - publish

variables:
  PIP_INDEX_URL: https://zelenjak.arnes.si/nexus/repository/ArnesGroupPyPI/simple

lint:
  stage: test_code
  image: zelenjak.arnes.si:5000/python:3.6
  script:
    - pip install poetry
    - poetry install
    - poetry run flake8
    - poetry run black --diff --check .
    #- poetry run pydocstyle fortiwlc_exporter

test_unit:
  stage: test_code
  image: zelenjak.arnes.si:5000/python:3.6
  script:
    - pip install poetry
    - poetry install
    - poetry run pytest --cov=fortiwlc_exporter tests

build_rpm:
  stage: build
  image: zelenjak.arnes.si:5000/arnes/rpmbuild:7
  artifacts:
    paths:
      - rpms/
      - srpms/
    expire_in: 1 hour
  variables:
    LC_ALL: en_US.utf8
  script:
    - yum install -y python34
    - python3.4 -m ensurepip --default-pip
    - pip3.4 install --upgrade bumpversion poetry pyinstaller
    - poetry install --no-dev
    - poetry run pip freeze | grep -v egg=fortiwlc_exporter > requirements.txt
    - pip3.4 install -r requirements.txt
    - ./dev-version.sh
    - ./build.sh

test_rpm:
  image: zelenjak.arnes.si:5000/arnes/centos:7
  stage: test_package
  dependencies:
    - build_rpm
  script:
    - yum install -y rpms/*/fortiwlc-exporter*.rpm
    - "[ -f /etc/fortiwlc_exporter.ini ]"
    - "fortiwlc_exporter --version"
    - "fortiwlc_exporter -c /etc/fortiwlc_exporter.ini &"
    - "sleep 2"
    - "curl --fail 'http://localhost:9118/'"

publish_rpm:
  image: zelenjak.arnes.si:5000/arnes/centos:7
  stage: publish
  script:
    - ls -lR ./rpms/
    - >-
      curl -f -u "${NEXUS_USERNAME}:${NEXUS_PASSWORD}"
      --upload-file ./rpms/x86_64/fortiwlc-exporter-*.el7.x86_64.rpm
      https://zelenjak.arnes.si/nexus/repository/ArnesYumCentos7/x86_64/
  only:
    - tags
